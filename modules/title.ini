[module/title]
type = internal/xwindow
format = <label>
format-prefix = ﯑
format-background = ${theme.title-bg}
format-padding = 1

label = " %title%"
label-maxlen = 20

; Used instead of label when there is no window title
label-empty = " Desktop"
label-empty-foreground = ${color.fg}

